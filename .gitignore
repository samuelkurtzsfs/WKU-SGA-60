.DS_Store
__pycache__/
*.pyc
.vercel

# generated copies of archive files - rebuilt on every deploy
site/legislation/
site/docs/

# generated officer pages - one per person, rebuilt on every deploy and
# potentially thousands of files once the chamber rolls land
site/o/

# the roster workbook is generated from data/years.json
SGA-60-roster.xlsx

# scratch copies of the repository, one per research agent working in
# parallel, created and removed by the tooling that runs them
.claude/worktrees/

# Photo-hunt worklists are derived from years.json and photos.json each run.
# The findings files are kept: they carry the caption that identifies a face.
data/photo-finds/_worklist-*.json
